# Core
torch>=2.0
torchaudio
torchvision        # plot_to_image (spectrogram logging); pulls in Pillow
numpy<2
pandas             # preprocess_dataset_fma.py (reads FMA tracks.csv)
descript-audio-codec
soundfile
omegaconf
matplotlib
tensorboard
tqdm
scipy

# Metrics: audio FADs (optional, only if enabled in metrics.enabled)
encodec        # fad_encodec
resampy        # fad_vggish (VGGish log-mel front-end)
# fad_vggish also loads VGGish via torch.hub (harritaylor/torchvggish):
# the weights are downloaded on first use and cached under ~/.cache/torch/hub
# (needs network access once; pre-fetch for offline GPU nodes).
